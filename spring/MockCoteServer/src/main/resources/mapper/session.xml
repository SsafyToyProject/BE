<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mockcote.MockCoteServer.model.mapper.SessionMapper">
   <select id="searchReadySessions" resultMap="session">
        SELECT session_id, study_id, query_id, start_at, end_at, problem_pool
        FROM sessions
        WHERE NOW() BETWEEN DATE_SUB(start_at, INTERVAL 3 MINUTE) AND start_at
    </select>

    <select id="getProblemCount" parameterType="int" resultType="int">
        SELECT count(problem_id)
        FROM session_problems
        WHERE session_id = #{sessionId}
    </select>
    
    <select id="getParticipants" parameterType="int" resultType="int">
    	SELECT user_id
    	FROM session_participants
    	WHERE session_id = ${sessionId}
    </select>
</mapper>