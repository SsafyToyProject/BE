<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mockcote.MockCoteServer.domain.session.model.mapper.SessionMapper">
   
   <!-- 특정 스터디의 모든 세션 정보 조회 -->
   <select id="findSessionsByStudyId" resultType="session">
        SELECT session_id, study_id, query_id, start_at, end_at, problem_pool
        FROM sessions
        WHERE study_id = #{studyId}
        ORDER BY end_at DESC
    </select>


   <select id="searchReadySessions" resultType="session">
        SELECT session_id, study_id, query_id, start_at, end_at, problem_pool
        FROM sessions
        WHERE NOW() BETWEEN DATE_SUB(start_at, INTERVAL 3 MINUTE) AND start_at
    </select>
    
    <select id="searchActiveSessions" resultType="session">
    	SELECT session_id, study_id, query_id, start_at, end_at, problem_pool
        FROM sessions
        WHERE NOW() BETWEEN start_at AND end_at;
    </select>

    <select id="getProblemCount" parameterType="int" resultType="int">
        SELECT count(problem_id)
        FROM session_problems
        WHERE session_id = #{sessionId}
    </select>
    
    <select id="getParticipants" parameterType="int" resultType="int">
    	SELECT user_id
    	FROM session_participants
    	WHERE session_id = ${sessionId}
    </select>
	
	<select id="getSessionProblemIds" parameterType="int" resultType="int">
		SELECT problem_id
		FROM session_problems
		WHERE session_id=#{sessionId}
	</select>

</mapper>