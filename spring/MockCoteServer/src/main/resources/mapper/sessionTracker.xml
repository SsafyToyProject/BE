<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mockcote.MockCoteServer.model.mapper.SessionTrackerMapper">
   
   
   <select id="findSessionTracker" resultType="sessionTracker">
        SELECT session_id, user_id, problem_id, solved_at, performance, language, code_link, description
        FROM session_trackers
        WHERE session_id = #{sessionId} AND user_id = #{userId} AND problem_id = #{problemId}
    </select>
</mapper>